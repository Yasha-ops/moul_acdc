exception Not_Done

let draw_fill_square (x,y) size color =
    Printf.printf "Function {draw_fill_square} not done !";
 raise Not_Done;;


let draw_cell (x,y) size color =
    Printf.printf "Function {draw_cell} not done !";
 raise Not_Done;;


let draw_board board size =
    Printf.printf "Function {draw_board} not done !";
 raise Not_Done;;


let rules0 cell near =
    Printf.printf "Function {rules0} not done !";
 raise Not_Done;;


let get_cell_neighborhood (x,y) board =
    Printf.printf "Function {get_cell_neighborhood} not done !";
 raise Not_Done;;


let rec cell_count =
    Printf.printf "Function {cell_count} not done !";
 raise Not_Done;;


let count_neighbours (x,y) board size  =
    Printf.printf "Function {count_neighbours} not done !";
 raise Not_Done;;


let seed_life board size count =
    Printf.printf "Function {seed_life} not done !";
 raise Not_Done;;


let new_board size n =
    Printf.printf "Function {new_board} not done !";
 raise Not_Done;;


let next_generation board size =
    Printf.printf "Function {next_generation} not done !";
 raise Not_Done;;


let remaining board =
    Printf.printf "Function {remaining} not done !";
 raise Not_Done;;


let rec game board size n infty =
    Printf.printf "Function {game} not done !";
 raise Not_Done;;


let new_game size nb_cells n =
    Printf.printf "Function {new_game} not done !";
 raise Not_Done;;
